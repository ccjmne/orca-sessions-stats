export interface Instructor {
  empl_pk: number;
  empl_firstname: string;
  empl_surname: string;
  empl_dob: Date;
  empl_permanent: boolean;
  empl_address: string;
  empl_notes: string;
  empl_gender: boolean;
  empl_external_id: string;
}
